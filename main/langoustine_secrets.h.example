/*
 * Langoustine Build-time Secrets
 *
 * Copy this file to langoustine_secrets.h and fill in your values.
 * After any change, rebuild: idf.py fullclean && idf.py build
 *
 *   cp langoustine_secrets.h.example langoustine_secrets.h
 */

#pragma once

/* WiFi */
#define LANG_SECRET_WIFI_SSID       ""
#define LANG_SECRET_WIFI_PASS       ""

/* LLM API (Anthropic / OpenAI / OpenRouter) */
#define LANG_SECRET_API_KEY         ""
#define LANG_SECRET_MODEL           ""
#define LANG_SECRET_MODEL_PROVIDER  "anthropic"

/* HTTP Proxy (leave empty or set both) */
#define LANG_SECRET_PROXY_HOST      ""
#define LANG_SECRET_PROXY_PORT      ""

/* Brave Search API (for web_search tool) */
#define LANG_SECRET_SEARCH_KEY      ""

/* Telegram Bot (optional — can also use CLI: set_tg_token <TOKEN>) */
#define LANG_SECRET_TG_TOKEN        ""
